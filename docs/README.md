# boll doc site

The doc site's dependencies and Node version are currently managed separately from the rest of the repo due to the repo's outdated Node version (which should be preserved for compatibility until a major release).
